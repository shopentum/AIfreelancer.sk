/** @deprecated Import from @/config/defaultProjects or use useProjects(). */
export {
  DEFAULT_PROJECT_ID,
  DEFAULT_SEED_PROJECTS,
} from "@/config/defaultProjects";

export { getProjectLabel } from "@/domain/projectService";
